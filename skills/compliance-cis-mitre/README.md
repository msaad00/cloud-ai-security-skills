# compliance-cis-mitre/ (transition)

The benchmark and posture skills that used to live here now live under
[`../evaluation/`](../evaluation/).

Moved skills:

- `cspm-aws-cis-benchmark`
- `cspm-gcp-cis-benchmark`
- `cspm-azure-cis-benchmark`
- `k8s-security-benchmark`
- `container-security`

Keep external links working by updating them to `skills/evaluation/<skill-name>/`.
