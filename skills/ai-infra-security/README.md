# ai-infra-security/ (transition)

The AI-infrastructure posture and discovery skills that used to live here now
live under [`../evaluation/`](../evaluation/).

Moved skills:

- `model-serving-security`
- `gpu-cluster-security`
- `discover-environment`

Keep external links working by updating them to `skills/evaluation/<skill-name>/`.
