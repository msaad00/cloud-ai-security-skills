{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ingest-k8s-audit-ocsf MCP parameters",
  "description": "Typed CLI parameters for Kubernetes audit JSON/JSONL to OCSF API Activity ingestion.",
  "type": "object",
  "properties": {
    "input_path": {
      "type": "string",
      "description": "Path to raw Kubernetes audit JSON or JSONL. Omit to read stdin via the input field.",
      "minLength": 1,
      "x-cli-style": "positional"
    },
    "output_path": {
      "type": "string",
      "description": "Write OCSF JSONL to this path instead of stdout.",
      "minLength": 1,
      "x-cli-flag": "--output"
    }
  },
  "examples": [
    {
      "input": "{\"kind\":\"Event\",\"apiVersion\":\"audit.k8s.io/v1\",\"level\":\"RequestResponse\"}\n",
      "output_format": "ocsf"
    },
    {
      "input_path": "/tmp/k8s-audit.jsonl",
      "output_path": "/tmp/k8s-audit.ocsf.jsonl"
    }
  ]
}
